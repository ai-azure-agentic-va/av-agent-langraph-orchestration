# Prod Deployment TODO — `deep-agents-refactor`
---

## 🚦 Prod gate (P1 must-fix / risk-accept before deploy)

- [x] **AISEARCH-1** — import-time embeddings → startup crash *(Retrieval)*
- [x] **AISEARCH-3** — Azure Search has no request timeout → worker hang *(Retrieval)*
- [x] **SNCLIENT-2 / SNTOOLS-7** — date-only `*_before` drops the whole boundary day (wrong results) *(ServiceNow)*
- [x] **CONFIG-MAXSTEPS / AGENT-RECURSION** — agent loop has no enforced step ceiling *(Platform)*
- [x] **GRAPHGROUPS-LOCK** — global lock held across network fetch + unbounded group cache (memory leak) *(Platform)*
- [x] **AUTH-JWT-TEST** — zero test coverage on the JWT/JWKS auth path *(Security)*
- [ ] **SAFETY-LAYER** — regex-only gate, no per-tool authorization *(Security — L effort; risk-acceptable for a read-only deploy with sign-off, see note)*
- [~] **INTENT-1, INTENT-4, INTENT-5** (+INTENT-2) — core retrieval intents failing *(Behavior/Eval)* — INTENT-1 root cause (substring matching) fixed + tested; INTENT-4/5 prompt+doc fixes in place but unvalidated (no eval harness/real TC data); **INTENT-2 still untouched**

---

## 1. Retrieval / AI Search — `src/v1/core/tools/ai_search/ai_search.py`

- [x] **AISEARCH-1** · `P1` · `S` — **Import-time embeddings client → whole agent package fails to import.** ✅ Fixed: `_build_embeddings()` no longer runs at module top; replaced with a lazy double-checked-lock `_get_embeddings()` (mirrors `_get_search_client`), called from `_run_search`. `ai_search_tool` signature is now `top_k: int | None = None`, resolved to `settings.ai_search_default_top_k` in the body. Verified the module imports cleanly with Azure OpenAI creds unset.
- [x] **AISEARCH-3** · `P1` · `S` — **No request timeout on `SearchClient`.** ✅ Fixed: added `azure_search_timeout_seconds` (default 30s, `AZURE_SEARCH_TIMEOUT_SECONDS`) to config and pass it as both `connection_timeout` and `read_timeout` when building the cached `SearchClient` ([ai_search.py:_get_search_client](../../src/v1/core/tools/ai_search/ai_search.py#L80)). Caps the `to_thread` worker at 30s instead of the azure-core 300s default. Verified by `test_ai_search.py`. ⚠️ Residual: the embeddings call (`embed_query`) in the same worker still uses the OpenAI client's own default timeout — fold an `AzureOpenAIEmbeddings(timeout=...)` into AISEARCH-4 if full worker-hang protection is wanted.
- [ ] **AISEARCH-4** · `P2` · `S` — **Search/embeddings no longer wrapped in retry**; one transient 503 returns "Knowledge base search failed" ([:142](../../src/v1/core/tools/ai_search/ai_search.py#L142), [:150](../../src/v1/core/tools/ai_search/ai_search.py#L150)). **Fix:** wrap embed+search in retry (add a sync variant of `http_retry_async` in [retry.py](../../src/v1/utils/retry.py), or make the call an awaited coroutine).
- [ ] **AISEARCH-8** · `P2` · `M` — **Reranker-score floor removed.** `@search.reranker_score` is read only to pick the per-doc max, never filtered ([:165-190](../../src/v1/core/tools/ai_search/ai_search.py#L165)); `AZURE_SEARCH_RERANKER_SCORE_THRESHOLD=1.2` in `.env.example:50` is **read nowhere**. **Fix:** add the config field, drop sub-threshold docs (keep top-1), renumber citations. *(Also confirm semantic ranking is requested or the score is `None`.)*
- [ ] **AISEARCH-IDX** · `P3` · `S` — index name passed to `SearchClient` with no `validate_index_name()`; default-index fallback already prevents the blank case, so low impact. **Fix:** regex-validate, fall back to default on failure.
- [ ] **AISEARCH-CACHE** · `P3` · `M` — `embed_query` does a full Azure round-trip every call incl. repeats. **Fix:** bounded LRU keyed by `(query, deployment)`. *(Cost/latency, not correctness — sequence after the P1s.)*

## 2. ServiceNow Tools & Client — `tools.py` / `utils/clients/servicenow.py`

- [x] **SNCLIENT-2** · `P1` · `S` — **Date-only `*_before` silently drops the boundary day.** ✅ Fixed: `_matches_one`'s date branches now go through `_matches_date_bound`, which parses both sides to `datetime` and expands a date-only `*_before` to end-of-day (`23:59:59.999999`) so the whole boundary day is inclusive (matching `*_after`). Falls back to lexical compare only when a value is unparsable. Verified by `test_servicenow.py::test_date_only_*`.
- [x] **SNTOOLS-7** · `P2` · `S` — **Date-bound validation only checks digit shape.** ✅ Fixed: `_validate_date_bound` now validates with `datetime.strptime` against both accepted formats, so `2026-13-45` / `2026-05-10 99:99:99` / `2026-02-30` are rejected before reaching the query. *(Done as one date-handling change with SNCLIENT-2.)*
- [x] **SNTOOLS-MULTI** · `P2` · `S` — **Multi-status list ran N REST calls sequentially.** ✅ Fixed: the per-status queries fan out via `asyncio.gather` (shared token + `AsyncClient`, caller order preserved), so latency is the slowest call instead of the sum.
- [x] **SNTOOLS-STATUS** · `P2` · `M` — **Status normalization implemented twice with diverging rules.** ✅ Fixed: `_canonical_status` is now driven by `_STATE_DISPLAY_TO_STATUS` (the inverse of `_STATUS_TO_STATE`) with alias resolution + slug fallback, so input (`normalize_status`) and output round-trip through one source of truth. Verified by `test_status_normalizers_round_trip`.
- [x] **SNTOOLS-DISPLAY** · `P3` · `S` — `_display()` duplicated `_reference_value()`. ✅ Fixed: `tools.py` imports and uses `_reference_value`; the duplicate `_display` is gone.
- [x] **SNCLIENT-MATCHES** · `P3` · `S` — `_matches_one` priority/state branches were copy-paste. ✅ Fixed: extracted `_display_and_plain(raw) -> (display, plain)`; both branches now share one consistent scalar/reference rule.
- [x] **SNCLIENT-ENVDUP** · `P3` · `S` — `_env_bool`/`_env_float` duplicated across servicenow.py / auth.py / langsmith.py (`_truthy`). ✅ Fixed: promoted `truthy`/`env_bool`/`env_float` into [helper.py](../../src/v1/utils/helper.py); servicenow.py, auth.py, and langsmith.py now import them (local duplicates deleted).

## 3. Agent Harness / Platform — `agent.py` / `config.py` / `logging.py` / `helper.py` / `graph_groups.py`

- [x] **CONFIG-MAXSTEPS / AGENT-RECURSION** · `P1` · `M` — **Agent loop has no enforced step ceiling.** ✅ Fixed: `_build_agent_sync` now returns `agent.with_config({"recursion_limit": settings.agent_max_steps})` ([agent.py:_build_agent_sync](../../src/v1/core/agent.py#L132)), so `AGENT_MAX_STEPS` (default 50) is the authoritative ceiling instead of the LangGraph default (25). Verified `Pregel.with_config` returns a Pregel copy (not a RunnableBinding), so the platform's downstream checkpointer/store injection still works. Covered by `test_agent_recursion.py` (wiring + behavioral enforcement). *(Subagent recursion limits are a separate, deeper concern not in scope here.)*
- [x] **GRAPHGROUPS-LOCK** · `P1` · `M` — **Process-global lock held across the blocking, paged Graph fetch + unbounded `_cache`.** ✅ Fixed: `resolve_groups_via_graph` now holds `_cache_lock` only for the O(1) cache read and the write — `_fetch_groups` runs **outside** the lock, so distinct users no longer serialize on a cold cache (a rare duplicate fetch for the same cold oid is the accepted trade). `_cache` is now an `OrderedDict` bounded by a new `_store_locked` that does a TTL sweep and an LRU cap (`GRAPH_GROUPS_CACHE_MAX_ENTRIES`, default 10k). The fetch is already off the event loop (auth offloads `authenticate_authorization` via `anyio.to_thread`). Verified by `test_graph_groups.py` (concurrency-no-serialize, cache hit, LRU eviction, TTL sweep).
- [ ] **CONFIG-TOPP / AGENT-RECURSION (model kwargs)** · `P2` · `S` — `ai_llm_default_top_p` and `ai_llm_default_tmax_token` are now correctly typed but still **read nowhere** ([config.py:71-73](../../src/v1/core/config.py#L71); only `temperature` is threaded at [agent.py:37-42](../../src/v1/core/agent.py#L37)). **Fix:** thread `max_tokens`/`top_p` into `build_azure_chat_model` (pick one of temperature/top_p), **or** delete the dead knobs.
- [ ] **AGENT-PROFILE** · `P2` · `S` — `_HARNESS_PROFILE_KEYS = ("azure", "keywordplannermodel")` ([agent.py:156-159](../../src/v1/core/agent.py#L156)): `"keywordplannermodel"` is dead; `"azure"` is correct today but an unasserted magic string — if the model is swapped, summarization/prompt-caching exclusions silently re-activate. **Fix:** reduce to `("azure",)`, drop the stale comment; optionally assert via deepagents `_verify_excluded_middleware_coverage`.
- [ ] **CONFIG-CORS** · `P3` · `S` — `cors_origin_list` re-splits CSV inline though `_split_csv` is imported ([config.py:227](../../src/v1/core/config.py#L227)). **Fix:** `return _split_csv(value)`.
- [ ] **AGENT-PEP8** · `P3` · `S` — missing two-blank-line separation before `_EXCLUDED_MIDDLEWARE` and `close_agent_resources` ([agent.py:146-147, 185-186](../../src/v1/core/agent.py#L146)). **Fix:** run `ruff format`/`black`.
- [ ] **LOGGING-HANDLERS** · `P2` · `M` — ⚠️ **`logging.py` is currently dead/unwired** (never `dictConfig`'d, not imported anywhere), so the "7 sync handlers / DEBUG root / blocking disk I/O" concern ([logging.py:203-214](../../src/v1/utils/logging.py#L203)) is **latent**. **Decide first:** if logging is meant to be active, wire `dictConfig` at startup **and** fix it (QueueHandler/QueueListener, root→INFO); otherwise **delete the module**. Don't ship a half-wired logging stack.
- [ ] **HELPER-SANITIZE** · `P3` · `S` — `sanitize_for_streaming` duplicates `sanitize_for_logging` (and is **dead code** — no callers, [helper.py:166-200](../../src/v1/utils/helper.py#L166)). **Fix:** collapse into one parameterized worker, or delete until wired.
- [ ] **LANGSMITH-REQID** · `P3` · `S` — ⚠️ **documentation defect, not a regression.** `log_extra` docstring claims auto-attach of `request_id` but the contextvar infra doesn't exist and `log_extra` has **no callers** ([langsmith.py:29-41](../../src/v1/utils/langsmith.py#L29)). **Fix:** correct the docstring (don't "re-import request_context" — there's nothing to import), or wire a real `current_request_id` contextvar if correlation is wanted.

## 4. Security & Auth — `safety.py` / `auth.py`

- [x] **AUTH-JWT-TEST** · `P1` · `M` — **Zero test coverage on the security-critical JWT/JWKS path.** ✅ Fixed: added [test_auth.py](../../src/v1/test/v1/utils/test_auth.py) — 35 tests covering signature verify (RS256/ES256 accept; `alg=none`, disallowed-alg, wrong-kid, multi-key-no-kid, wrong-key, tampered-payload reject), claim validation (exp/nbf/iss/aud/tid + skew + dev-vs-prod exp requirement), the non-dev `validate_for_environment` hard-guards (dev-in-prod, missing JWKS/issuer/audience/scopes, non-https JWKS), the JWKS cache (json path, fetch-once, expiry refetch, https guard), and end-to-end authenticate accept/reject. Keys generated in-process with `cryptography`; tokens/JWKs via PyJWT; the one JWKS-URL test stubs `urlopen` — **no network**. *(The pre-existing 0-byte test file at `src/v1/test/v1/api/test_one_message.py` is unrelated and still empty.)*
- [ ] **SAFETY-LAYER** · `P1` · `L` — **Regex gate over the last human message only**, blind to tool args / subagent inputs; ServiceNow tools have **no per-tool authorization/HITL** ([safety.py:88-99](../../src/v1/core/middlewares/safety.py#L88)). Read-only today, so **risk-acceptable for an initial deploy with explicit sign-off**, but is the deeper fix for "don't run destructive actions." **Fix:** per-tool authz / HITL interrupt on any write action; managed content-safety classifier (Azure AI Content Safety) for input **and** output; keep regex only as a cheap pre-filter. *(Replaces the old "harden safety gate" TODO line — that comment is already gone from the rewritten file.)*
- [ ] **SAFETY-DLP** · `P2` · `S` — **`OUTPUT_REDACTIONS` (SSN/card/secret) defined but never applied** — no `after_model` hook, so output DLP is a silent no-op ([safety.py:59-70](../../src/v1/core/middlewares/safety.py#L59)). In a banking context this invites false confidence. **Fix:** wire an `aafter_model` hook (can reuse `helper.redact_text`), **or** delete the block.
- [ ] **SAFETY-PRINT** · `P3` · `S` — stray `print("Safety Gate: Didn't block request.")` on every safe request ([safety.py:98](../../src/v1/core/middlewares/safety.py#L98)). **Fix:** `logger.debug` or remove.
- [ ] **SAFETY-INIT** · `P3` · `S` — no-op `def __init__(self): pass` ([safety.py:84-85](../../src/v1/core/middlewares/safety.py#L84)). **Fix:** delete.
- [ ] **SAFETY-IMPORTS** · `P3` · `S` — `List` imported twice + several unused imports ([safety.py:1-16](../../src/v1/core/middlewares/safety.py#L1)). **Fix:** trim.
- [ ] **AUTH-CSV** · `P3` · `S` — `_csv()` re-implements CSV split with an inline `import re` ([auth.py:627-632](../../src/v1/utils/auth.py#L627)). **Fix:** lift `import re` to module top; optionally delegate to a shared splitter (note: `_csv` splits on whitespace+commas, `_split_csv` only commas — don't change semantics).
- [ ] **AUTH-JWKS** · `P3` · `S` — `_load_jwks` re-validates the JWKS-URL scheme already checked at config build ([auth.py:446-457](../../src/v1/utils/auth.py#L446)); duplication for non-dev only (it's the *only* guard for dev). **Fix:** centralize scheme validation in `validate_for_environment` for all envs, then let `_load_jwks` assume a vetted URL.

## 5. Agent Behavior / Eval — ServiceNow intent failures (from [TODO.md](./TODO.md) test status)

> Root causes triaged against `tools.py`, `clients/servicenow.py`, and the orchestrator/subagent prompts. **INTENT-1/5/7 share one root cause:** contiguous-substring matching over a narrow haystack ([servicenow.py:545-554](../../src/v1/utils/clients/servicenow.py#L545)) — fix that first and re-run the evals before chasing the rest.
>
> **Status (2026-06-17):** The shared root cause is fixed in code (tokenized AND-of-substrings) and unit-tested against the bundled fixture. The prompt/field-doc fixes for INTENT-3/4/5/6 are in place but **cannot be validated end-to-end** — the real TC-XXX cases (query → expected ticket IDs) are **not in the repo** (only prose in [TODO.md](./TODO.md)), so there is still no runnable eval harness. INTENT-2 (long-history) is untouched. Treat the prompt-only items as best-effort until the harness + real test data exist.

- [x] **INTENT-1** · `P1` · `M` — *Get incidents for datasource X* (TC-008/010/015/019). ✅ Code root cause fixed: `_matches_one`'s `description_contains` (and every `*_contains`) now uses `_contains_all_tokens` — tokenized **AND-of-substrings** instead of a contiguous match, so `'missing data'` matches tickets containing both words (0→2 on the bundled fixture), `'transaction ledger'` matches, order-independent. Verified by `test_servicenow_intents.py`. Prompt now also tells the subagent to use `description_contains` for datasource lookups and to retry with bare keyword / `cause_contains` / `configuration_item_contains` before reporting "none." ⚠️ End-to-end (TC-008 etc.) unverified — no eval harness / real test data in repo.
- [~] **INTENT-5** · `P1` · `M` — *Missing data incidents* (TC-047). Code root cause covered by INTENT-1 (tokenization). Prompt now lists the "missing data" wording variants (missing / not loading / truncated / partial load / row-count mismatch / stale / delayed / lag) and says to try several and merge. ⚠️ Prompt behavior unvalidated without the eval harness.
- [~] **INTENT-4** · `P1` · `M` — *Pipeline incidents* (TC-037/040). Addressed via prompt + field doc (no client code change): the `statuses` field doc now states `'open'` includes Resolved and to pass `new,in_progress,on_hold` for unresolved-only (A); the subagent prompt gives the anchored `configuration_item_contains='PL-'` rule for pipeline-infra failures and says a DQ incident merely mentioning a pipeline (CI=`'Data Quality'`) is not infra (B). ⚠️ Prompt-only, unvalidated; no not-resolved *filter* was added to the client.
- [ ] **INTENT-2** · `P1` · `investigation` — *Engineer lookup fails in long-history threads* (TC-031/036). Likely orchestrator answering from prior turns / dropping the ai_search→servicenow bridge under context pressure ([orchestrator.py:43-60](../../src/v1/core/prompts/orchestrator.py#L43)). **Fix:** reproduce with long synthetic history + capture trace; reposition the "always re-run for the CURRENT question" rule; consider trimming/summarizing prior turns.
- [x] **INTENT-7** · `P2` · `M` — *Historical resolution for similar INC* (TC-042). ✅ Root cause was **over-constrained filters**: the agent ANDed the source incident's literal cause (`'Authentication issue'`) onto the data-source search, so the real historical match (INC3011201, cause `'Source timeout'`) was dropped → false "none found" (filters AND together; the tool can't OR two cause strings). The subagent prompt now spells out the explicit two-step recipe: (1) `servicenow_get_ticket_detail` on the referenced INC to read its data source, then (2) `servicenow_list_tickets` with `statuses='resolved,closed'` + `description_contains=<data source>` and **no** cause/`configuration_item` over-constraint — judge cause *type* by reading results — then return `resolution_notes` and suggest analogous steps. Pinned by `test_similar_incident_overconstrained_cause_finds_nothing` / `test_similar_incident_by_datasource_finds_historical_match` against the bundled fixture (INC3235130 → INC3011201). ⚠️ End-to-end (TC-042 through the live agent) still unverified — no eval harness in repo.
- [~] **INTENT-3** · `P2` · `S` — *Engineer via wrong field* (TC-035). ✅ Prompt now states datasource/engineer lookups use `description_contains` then read `assigned_to`/`resolved_by`, and to **never** use `configuration_item_contains` for them (most incidents share CI=`'Data Quality'`). ⚠️ Field descriptions left mostly as-is to avoid schema bloat; unvalidated.
- [~] **INTENT-6** · `P2` · `investigation` — *Incidents last month by cause* (TC-53). Prompt now says to prefer `cause_contains` (substring) over the exact-match `cause` filter for free-form wording. Date handling already hardened (SNCLIENT-2). ⚠️ Canonical-cause enumeration not added; unvalidated.
- [ ] **INTENT-2** · `P1` · `investigation` — *Engineer lookup fails in long-history threads* (TC-031/036). **Untouched** — needs reproduction with a long synthetic history + trace capture, which requires running the agent (no harness yet). Likely orchestrator answering from prior turns / dropping the ai_search→servicenow bridge under context pressure.
- [ ] **Eval harness** · `P1` · `M` — still open. A fixture-based regression test for the substring root cause was added ([test_servicenow_intents.py](../../src/v1/test/v1/utils/test_servicenow_intents.py)), but the **real TC-008/010/.../053 cases (query → expected ticket IDs) are not in the repo** — only prose in [TODO.md](./TODO.md). Building the full regression suite needs that test data.

---

## Notes & caveats

- **`docs/fixes/Service now fixed.md` is currently empty (0 bytes).** If it's meant to log completed ServiceNow fixes, populate it and I'll cross-reference so we don't double-list done work. Nothing is recorded as fixed there yet.
- **Dead-but-present code** flagged by verification (don't over-invest as if production-live): `logging.py` (LOGGING-HANDLERS — never `dictConfig`'d), `log_extra` (LANGSMITH-REQID — no callers), `sanitize_for_streaming` (HELPER-SANITIZE — no callers). Decide "wire or delete" rather than polishing.
- **Open `src/v1/TODO.md` items mapped here:** "Jwt auth test"→AUTH-JWT-TEST · "Intent fixes"→§5 · "memory Leaks"→GRAPHGROUPS-LOCK · "Ai search tool improvement"→§1 · "Safety gate hardening"→SAFETY-LAYER · "Add groups… mapping change… remove default"→ covered by TENANT_GROUP_* routing touched in AISEARCH-IDX/STARTER-5 (confirm intent with author).
- **STARTER-5** · `P2` · `S` *(API)* — `StarterPrompt(**prompt)` over admin config has no `try/except` ([starter_prompts.py:83-85](../../src/v1/api/routes/starter_prompts.py#L83)); a malformed `TENANT_GROUP_STARTER_PROMPTS_MAPPING` entry 500s the endpoint instead of falling back to `FALLBACK_PROMPTS`. **Fix:** guard per-entry (skip+log), fall back; ideally validate the mapping at config load.
